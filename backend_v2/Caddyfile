# Elastic IP: Redirect HTTP to canonical domain (never attempt HTTPS on IP)
http://54.206.128.36 {
    redir https://api.callerooapp.com{uri} 308
}

api.callerooapp.com {
    encode gzip
    reverse_proxy api:8000
}

scheduler.callerooapp.com {
    encode gzip
    reverse_proxy scheduler:8090
}
